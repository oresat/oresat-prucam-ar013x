# PRU firmware
## Build and deploy PRU firmware
- Install the TI's PRU compiler.
    - `$ sudo apt install ti-pru-cgt-installer`
- Build and deploy pru firmware
    - `$ cd pru_code`
    - `$ sudo ./deploy.sh`
